const express = require("express");
const router = express.Router();

const anapecCtrl = require("../controllers/scrappingAnapec");

//populate dababase
// router.get("/anapec_html", anapecCtrl.anapec_html);

module.exports = router;
